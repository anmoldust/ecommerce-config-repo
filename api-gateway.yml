spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/auth/**
            
management:
  endpoints:
    web:
      exposure:
        include: "*"

management.endpoint.gateway.enabled: true
