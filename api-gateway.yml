spring:
  cloud:
    gateway:
      routes:
        - id: test-route
          uri: http://httpbin.org
          predicates:
            - Path=/test/**
          filters:
            - StripPrefix=1
            
management:
  endpoints:
    web:
      exposure:
        include: "*"

management.endpoint.gateway.enabled: true
